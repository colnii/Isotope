# v0.2 Demo Acceptance

状态：`accepted as v0.2 developer demo`

## 1. Purpose

本文记录当前 Isotope 是否满足 `v0.2 developer demo` acceptance。

这不是 production release 决策，不发布 GitHub Release。本文固化当前证据、能力范围、非目标和 tag 状态。

## 2. Acceptance Judgment

当前 mainline 满足 `v0.2 developer demo` acceptance。

证据：

- local full regression at the acceptance anchor: `735 passed`
- v0.1 demo plain text: success
- v0.1 demo JSON: success
- v0.2 demo plain text: success
- v0.2 demo JSON: success
- CI smoke workflow 已配置，覆盖 full tests 和 demo plain / JSON smoke
- no `x_agent.*` imports
- `/home/lumber/Github/x-agent` untouched

## 3. Accepted Commands

v0.1 compatible demo:

```bash
python -m isotope.demo
python -m isotope.demo --json
```

v0.2 scenario:

```bash
python -m isotope.demo --scenario v0.2
python -m isotope.demo --scenario v0.2 --json
```

## 4. What This Demo Proves

Current v0.2 developer demo proves:

- canonical event log remains the source of truth.
- event replay can rebuild projected state.
- checkpoint-assisted rebuild can recover an equivalent read model.
- in-process `HttpApiApp` facade can drive session / run / input / state / events / artifact summary paths.
- approval pause / resume boundary works: pending approval can be approved, then resumes through the existing executor path.
- controlled artifact content policy works at retrieval layer with structured `ResourceRef`, explicit grants, caller context, and purpose.
- memory remains `boundary_only`; memory read-model / checkpoint boundaries exist, but durable memory storage/query is not implemented.

## 5. What This Demo Does Not Prove

Current v0.2 developer demo does not prove:

- real HTTP server or network listener.
- real LLM integration.
- durable memory storage.
- real memory query engine.
- external ingestion / `ImportedSnapshot`.
- production auth / identity.
- approval UI / notification / timeout scheduler.
- hosted deployment or production release packaging.
- public HTTP full-content artifact route.

## 6. Tag Status

`v0.2-demo` lightweight tag 已创建并推送。

当前 tag 状态：

- local tag: `v0.2-demo`
- remote tag: `v0.2-demo`
- tag commit: `09319e7407116d9f99f4a18853d4df23a8714720`
- local and remote tag point to the same commit.
- GitHub Release has not been published.
- This is a developer demo tag, not a product release.

Post-tag verification evidence:

- `735 passed` was the v0.2 demo tag acceptance anchor; current mainline is `765 passed` after Track F boundary / read-model / checkpoint slices.
- v0.1 and v0.2 demo commands still succeed.
- no `x_agent.*` imports.
- `/home/lumber/Github/x-agent` remains untouched.

## 7. Next Possible Steps

- Prepare a GitHub Release draft for `v0.2-demo`, but do not publish without explicit request.
- Track F has since landed and closed for now at boundary / read-model / checkpoint scope; provider adapters, webhooks, and public ingestion API remain deferred.
- Post-tag delta is recorded in `../../reviews/post-v0.2-tag-delta.md`. Do not move `v0.2-demo`; only prepare `v0.2.1-demo` if a reviewer needs a fixed tag that includes Track F.
