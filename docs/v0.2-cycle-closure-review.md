# v0.2 Cycle Closure Review

状态：`closed for current implementation cycle`

## 1. Purpose

本文记录当前 v0.2 implementation cycle 的 closure 判断。

本 review 不创建 tag、不发布 GitHub Release、不打开新 track，也不改变 `v0.2-demo` tag。它只说明当前阶段已经完成什么、仍然 deferred 什么，以及下一步应优先做什么。

## 2. Current Anchors

- `v0.2-demo` tag: `09319e7407116d9f99f4a18853d4df23a8714720`
- current `main` at review start: `68ea007bff9a1c7f44dff6b6806939eeec1b4eb9`
- current baseline: `820 passed`
- v0.1 demo plain / JSON: expected success
- v0.2 demo plain / JSON: expected success
- GitHub Release: not published
- `v0.2.1-demo`: not created

`main` is ahead of `v0.2-demo`; the post-tag delta is recorded in `docs/post-v0.2-tag-delta.md`.

## 3. Closure Judgment

The current v0.2 implementation cycle can pause.

The repo has moved from the original v0.1 kernel-loop developer demo into a broader v0.2 developer-demo slice with:

- in-process HTTP facade boundary
- controlled artifact content read policy
- approval pause / resume boundary
- external ingestion / `ImportedSnapshot` boundary
- v0.2 demo scenario showing Track A / C / E
- Track F boundary / read-model / checkpoint support on `main`

This is enough for a stable developer-review checkpoint. The project should not keep opening runtime tracks by default.

## 4. Closed Tracks

Closed for now:

- Track D / H: Demo / Docs Polish and v0.2 demo scenario.
- Track A: HTTP API Minimal Surface.
- Track C: Artifact Content Read Policy / Controlled Full-Content Retrieval.
- Track E: Approval Pause / Resume Boundary.
- Track F: External Ingestion / `ImportedSnapshot` Boundary.

These tracks are closed at boundary / read-model / demo-scenario scope, not at full product scope.

## 5. Deferred Areas

Still deferred:

- real listening HTTP server
- real LLM integration
- durable memory storage
- real memory query engine
- provider adapters
- external webhooks / callbacks
- public external ingestion HTTP API
- auth / identity / multi-user
- approval UI / notification / timeout scheduler
- complex approval policy DSL
- semantic retrieval / ranking
- memory controlled expand implementation
- hosted deployment / production release packaging

Do not implement these without a new design note and red tests.

## 6. Tag / Release Recommendation

Do not move or force-update `v0.2-demo`.

Default recommendation:

- Keep `v0.2-demo` as the accepted developer demo anchor.
- Keep `main` ahead of the tag with Track F, Agent / Worker lifecycle, Workspace substrate, and Retry / Cancel / Supersede first slices documented.
- Do not create `v0.2.1-demo` unless an external reviewer needs a fixed tag that includes these post-tag first slices.
- Do not publish GitHub Release without a separate explicit request.

## 7. Recommended Next Mode

Recommended next mode: cleanup / docs organization / external review.

Suggested next steps:

1. Prepare a docs directory migration execution plan.
2. Keep implementation paused unless a specific reviewer gap appears.
3. If a fixed Track F anchor is needed, run `v0.2.1-demo` tag readiness check only; do not create the tag without explicit approval.
4. Keep Track B real HTTP adapter, Track G memory query, and real integrations deferred.

## 8. Safe Cleanup Scope

Safe next cleanup work:

- docs directory migration plan
- link map and stub strategy
- README / docs reading-order polish
- external-review checklist
- optional release draft text for `v0.2-demo` or future `v0.2.1-demo`

Unsafe without a new track:

- provider adapter implementation
- real HTTP listener
- memory query engine
- production auth
- approval product UI
- semantic retrieval / ranking

## 9. Verification Baseline

Expected verification for this closure:

```bash
PYTHONPATH=src .venv/bin/python -m pytest tests/isotope_kernel -q
PYTHONPATH=src .venv/bin/python -m isotope_kernel.demo
PYTHONPATH=src .venv/bin/python -m isotope_kernel.demo --json
PYTHONPATH=src .venv/bin/python -m isotope_kernel.demo --scenario v0.2
PYTHONPATH=src .venv/bin/python -m isotope_kernel.demo --scenario v0.2 --json
```

Expected full regression baseline after later mainline first slices: `820 passed`.
