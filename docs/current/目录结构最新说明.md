# 目录结构最新说明

状态：当前目录快照。更新时间：2026-05-24。

本文记录当前 Git tracked（已纳入版本管理）的目录和文件用途。
呈现格式按“目录树 + 行尾中文注释”组织，便于像读代码结构一样快速扫一遍。

## 生成范围

- 文件数量：581 个 tracked 文件。
- 目录数量：52 个目录，包含根目录。
- 已排除：`.git/`、`.venv/`、`.worktrees/`、`.pytest_cache/`、`__pycache__/`、`*.egg-info/`、本地私有配置 `src/isotope/features/supervisor/supervisor_llm_pool.toml`。
- 结构命令：`.venv/bin/structure . -e .venv -e .git -e .pytest_cache -e .worktrees -e '*.egg-info' -e '__pycache__' -e 'supervisor_llm_pool.toml' --no-color -s`。
- 精确清单命令：`git -c core.quotePath=false ls-files`。

## 快速读法

- `apps/` 是薄入口：CLI/API 只负责接用户输入，再转到 `src/isotope/`。
- `src/isotope/` 是主实现：按 core、features、platform、llm、runtime、integrations 等层次拆分。
- `tests/isotope/` 是回归测试：大量 `test_*` 文件对应源码里的边界、功能和行为。
- `docs/current/` 是当前事实入口；`docs/architecture/`、`docs/features/`、`docs/reviews/` 是参考和证据；`docs/archive/` 是历史材料。

## 完整目录说明

格式：`路径/文件名  # 中文用途`。英文 technical term（技术词）在必要时保留，便于搜代码。

```text
isotope/  # Isotope 仓库根目录，放项目说明、配置、应用入口、源码、测试和文档。
├── .github/  # GitHub 平台配置目录，当前主要放 CI 工作流。
│   └── workflows/  # GitHub Actions 工作流目录，用于自动跑测试和仓库检查。
│       └── ci.yml  # GitHub Actions CI 配置，定义远端自动测试流程。
├── apps/  # 面向用户或外部系统的薄入口目录，负责把命令/API 转到 src/isotope。
│   ├── api/  # HTTP API 应用入口目录，负责启动或暴露 Isotope 服务接口。
│   │   ├── README.md  # API 应用入口说明，解释如何使用 apps/api。
│   │   └── isotope_api.py  # API 启动脚本，把外部 HTTP 入口转发到 isotope.apps.api。
│   └── cli/  # CLI（命令行界面）入口目录，每个脚本对应一个可执行命令。
│       ├── README.md  # CLI 命令入口说明，索引各个 isotope-* 命令。
│       ├── isotope_ask.py  # isotope-ask CLI 入口，用于发起 ask/问答类产品流程。
│       ├── isotope_capability.py  # isotope-capability CLI 入口，用于查询或运行 capability（能力）。
│       ├── isotope_demo.py  # isotope-demo CLI 入口，用于运行 demo 场景。
│       ├── isotope_file.py  # isotope-file CLI 入口，用于文件功能。
│       ├── isotope_llm_smoke.py  # isotope-llm-smoke CLI 入口，用于 LLM 配置冒烟检查。
│       ├── isotope_project.py  # isotope-project CLI 入口，用于项目/workspace 功能。
│       ├── isotope_search.py  # isotope-search CLI 入口，用于搜索功能。
│       ├── isotope_supervisor.py  # isotope-supervisor CLI 入口，用于 Supervisor 扫描、看板和 worker 管理。
│       ├── isotope_task.py  # isotope-task CLI 入口，用于任务功能。
│       └── isotope_workbench.py  # isotope-workbench CLI 入口，用于工作台功能。
├── docs/  # 项目文档目录，按当前事实、架构、功能、评审和归档分层。
│   ├── architecture/  # 架构边界文档目录，记录 contract（契约）、guardrail（护栏）和版本化设计。
│   │   ├── action-type-registry-v0.1.md  # 架构说明文档，记录 动作、type、registry（注册表） 的设计背景和边界。
│   │   ├── agent-loop-planner-adapter-boundary-v0.2.md  # 架构边界文档，定义 Agent（智能体）、循环执行、planner（规划器）、adapter、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── agent-loop-run-control-boundary-v0.2.md  # 架构边界文档，定义 Agent（智能体）、循环执行、run、运行控制、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── agent-loop-step-driver-boundary-v0.2.md  # 架构边界文档，定义 Agent（智能体）、循环执行、执行步骤、driver、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── agent-loop-tick-policy-boundary-v0.2.md  # 架构边界文档，定义 Agent（智能体）、循环执行、tick、policy（策略）、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── agent-worker-lifecycle-boundary-v0.2.md  # 架构边界文档，定义 Agent（智能体）、worker（工作进程）、生命周期、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── approval-pause-resume-boundary-v0.2.md  # 架构边界文档，定义 审批、pause、resume、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── artifact-content-read-policy-v0.2.md  # 架构说明文档，记录 artifact（产物）、content、read、policy（策略） 的设计背景和边界。
│   │   ├── artifact-review-provenance-helper-boundary-v0.2.md  # 架构评审文档，复盘 artifact（产物）、评审、provenance、helper、边界契约 的实现闭环和剩余风险。
│   │   ├── capability-hub-core-boundary-v0.2.md  # 架构边界文档，定义 capability（能力）、hub、core、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── capability-runner-cli-boundary-v0.2.md  # 架构边界文档，定义 capability（能力）、运行入口、cli、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── capability-runner-thin-shell-boundary-v0.2.md  # 架构边界文档，定义 capability（能力）、运行入口、thin、shell、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── capability-search-launch-plan-boundary-v0.2.md  # 架构边界文档，定义 capability（能力）、搜索、launch、plan、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── checkpoint-history-fallback-v0.1.md  # 架构说明文档，记录 checkpoint（检查点）、history、fallback 的设计背景和边界。
│   │   ├── checkpoint-history-index-retention-v0.1.md  # 架构说明文档，记录 checkpoint（检查点）、history、index、retention 的设计背景和边界。
│   │   ├── checkpoint-history-save-boundary-v0.1.md  # 架构边界文档，定义 checkpoint（检查点）、history、save、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── checkpoint-integrity-v0.1.md  # 架构说明文档，记录 checkpoint（检查点）、integrity 的设计背景和边界。
│   │   ├── checkpoint-migration-versioning-v0.1.md  # 架构说明文档，记录 checkpoint（检查点）、migration、versioning 的设计背景和边界。
│   │   ├── checkpoint-ownership-v0.1.md  # 架构说明文档，记录 checkpoint（检查点）、ownership 的设计背景和边界。
│   │   ├── checkpoint-retention-compaction-v0.1.md  # 架构说明文档，记录 checkpoint（检查点）、retention、compaction 的设计背景和边界。
│   │   ├── checkpoint-save-trigger-v0.1.md  # 架构说明文档，记录 checkpoint（检查点）、save、trigger 的设计背景和边界。
│   │   ├── checkpoint-schema-version-fields-v0.1.md  # 架构边界文档，定义 checkpoint（检查点）、schema（数据结构约束）、version、fields 的 contract（契约）、输入输出和约束。
│   │   ├── checkpoint-v0.1-scope-freeze.md  # 架构说明文档，记录 checkpoint（检查点）、scope、freeze 的设计背景和边界。
│   │   ├── codex-as-tool-boundary-v0.2.md  # 架构边界文档，定义 Codex 集成、as、tool（工具）、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── commitment-levels.md  # 架构说明文档，记录 commitment、levels 的设计背景和边界。
│   │   ├── controlled-terminal-execution-boundary-v0.2.md  # 架构边界文档，定义 受控执行、终端执行、execution、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── error-taxonomy-boundary-v0.2.md  # 架构边界文档，定义 错误处理、taxonomy、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── error-taxonomy-closure-review.md  # 架构评审文档，复盘 错误处理、taxonomy、closure、评审 的实现闭环和剩余风险。
│   │   ├── event-envelope-schema-registry-v0.1.md  # 架构边界文档，定义 event（事件）、envelope、schema（数据结构约束）、registry（注册表） 的 contract（契约）、输入输出和约束。
│   │   ├── event-envelope-versioning-v0.1.md  # 架构说明文档，记录 event（事件）、envelope、versioning 的设计背景和边界。
│   │   ├── event-prefix-digest-v0.1.md  # 架构说明文档，记录 event（事件）、prefix、digest 的设计背景和边界。
│   │   ├── event-schema-registry-closure-review.md  # 架构评审文档，复盘 event（事件）、schema（数据结构约束）、registry（注册表）、closure、评审 的实现闭环和剩余风险。
│   │   ├── event-schema-registry-compatibility-boundary-v0.2.md  # 架构边界文档，定义 event（事件）、schema（数据结构约束）、registry（注册表）、compatibility、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── external-ingestion-boundary-v0.2.md  # 架构边界文档，定义 外部输入、数据摄取、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── http-api-minimal-surface-v0.2.md  # 架构说明文档，记录 HTTP API、api、minimal、surface 的设计背景和边界。
│   │   ├── kernel-architecture-v0.1.md  # 架构说明文档，记录 kernel（核心运行层）、architecture 的设计背景和边界。
│   │   ├── kernel-living-spec.md  # 架构说明文档，记录 kernel（核心运行层）、living、spec 的设计背景和边界。
│   │   ├── kernel-spec-v0.1.md  # 架构说明文档，记录 kernel（核心运行层）、spec 的设计背景和边界。
│   │   ├── llm-provider-tool-call-boundary-v0.2.md  # 架构边界文档，定义 LLM（大模型）、模型 provider（供应器）、tool（工具）、call、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── memory-record-persistence-boundary-v0.1.md  # 架构边界文档，定义 memory（记忆）、record、persistence、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── memory-v0.1-scope-freeze.md  # 架构说明文档，记录 memory（记忆）、scope、freeze 的设计背景和边界。
│   │   ├── memory-write-query-boundary-v0.1.md  # 架构边界文档，定义 memory（记忆）、write、query、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── model-tool-call-bridge-boundary-v0.2.md  # 架构边界文档，定义 模型、tool（工具）、call、bridge、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── planner-input-output-contract-v0.2.md  # 架构边界文档，定义 planner（规划器）、input、output、contract 的 contract（契约）、输入输出和约束。
│   │   ├── planner-runner-api-boundary-review.md  # 架构评审文档，复盘 planner（规划器）、运行入口、api、边界契约、评审 的实现闭环和剩余风险。
│   │   ├── policy-profile-action-registry-versioning-boundary-v0.2.md  # 架构边界文档，定义 policy（策略）、profile、动作、registry（注册表）、versioning、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── policy-registry-version-basis-closure-review.md  # 架构评审文档，复盘 policy（策略）、registry（注册表）、version、basis、closure、评审 的实现闭环和剩余风险。
│   │   ├── public-internal-docs-boundary.md  # 架构边界文档，定义 public、internal、docs、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── real-terminal-backend-boundary-v0.2.md  # 架构边界文档，定义 real、终端执行、backend、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── restart-write-helper-run-context-boundary-v0.2.md  # 架构边界文档，定义 重启恢复、write、helper、run、context、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── retry-cancel-supersede-boundary-v0.2.md  # 架构边界文档，定义 重试、cancel、替换旧动作、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── retry-cancel-supersede-runtime-integration-boundary-v0.2.md  # 架构边界文档，定义 重试、cancel、替换旧动作、runtime、集成、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── server-checkpoint-boundary-v0.1.md  # 架构边界文档，定义 服务端、checkpoint（检查点）、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── session-run-lifecycle-boundary-v0.2.md  # 架构边界文档，定义 session（会话）、run、生命周期、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── source-artifact-setup-helper-boundary-v0.2.md  # 架构边界文档，定义 源文件/来源、artifact（产物）、setup、helper、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── submit-action-helper-boundary-v0.2.md  # 架构边界文档，定义 提交动作、动作、helper、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── terminal-backend-adapter-contract-v0.2.md  # 架构边界文档，定义 终端执行、backend、adapter、contract 的 contract（契约）、输入输出和约束。
│   │   ├── terminal-backend-selection-boundary-v0.2.md  # 架构边界文档，定义 终端执行、backend、selection、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── terminal-capacity-system-runner-boundary-v0.2.md  # 架构边界文档，定义 终端执行、capacity、system、运行入口、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── tool-invocation-runtime-wiring-boundary-v0.2.md  # 架构边界文档，定义 tool（工具）、invocation、runtime、wiring、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── tool-protocol-boundary-v0.2.md  # 架构边界文档，定义 tool（工具）、protocol、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── tool-protocol-closure-review.md  # 架构评审文档，复盘 tool（工具）、protocol、closure、评审 的实现闭环和剩余风险。
│   │   ├── v0.2-roadmap.md  # 路线图文档，记录 v0.2、roadmap 的阶段目标。
│   │   ├── vcs-git-optional-boundary-v0.2.md  # 架构边界文档，定义 VCS（版本控制）、git、optional、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── worker-handoff-helper-boundary-v0.2.md  # 架构边界文档，定义 worker（工作进程）、handoff（移交）、helper、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── workspace-binding-helper-boundary-v0.2.md  # 架构边界文档，定义 workspace（工作区）、binding、helper、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── workspace-resource-lifecycle-boundary-v0.2.md  # 架构边界文档，定义 workspace（工作区）、resource、生命周期、边界契约 的 contract（契约）、输入输出和约束。
│   │   ├── workspace-resource-lifecycle-closure-review.md  # 架构评审文档，复盘 workspace（工作区）、resource、生命周期、closure、评审 的实现闭环和剩余风险。
│   │   └── workspace-substrate-boundary-v0.2.md  # 架构边界文档，定义 workspace（工作区）、substrate、边界契约 的 contract（契约）、输入输出和约束。
│   ├── archive/  # 历史归档目录，保存已过期但需要追溯的概念、草稿和旧说明。
│   │   ├── concepts/  # 早期概念和外部产品对比材料归档区，不作为当前规则。
│   │   │   ├── 2026-04-21-isotope-platform-kernel-reference-design.md  # 历史概念归档，保存 2026、04、21、isotope、platform、kernel（核心运行层）、reference、design 的早期设计或外部对比。
│   │   │   ├── 2026-04-22-isotope-first-study-companion-spec.md  # 历史概念归档，保存 2026、04、22、isotope、first、study、companion、spec 的早期设计或外部对比。
│   │   │   ├── 2026-04-22-isotope-marxist-leninist-study-agent-design.md  # 历史概念归档，保存 2026、04、22、isotope、marxist、leninist、study、Agent（智能体）、design 的早期设计或外部对比。
│   │   │   ├── 2026-04-22-isotope-persona-architecture.md  # 历史概念归档，保存 2026、04、22、isotope、persona、architecture 的早期设计或外部对比。
│   │   │   ├── 2026-04-22-isotope-study-agent-boundaries.md  # 历史概念归档，保存 2026、04、22、isotope、study、Agent（智能体）、boundaries 的早期设计或外部对比。
│   │   │   ├── 2026-04-22-isotope-vs-codex-claude-code-openclaw.md  # 历史概念归档，保存 2026、04、22、isotope、vs、Codex 集成、claude、code、openclaw 的早期设计或外部对比。
│   │   │   ├── 2026-04-22-isotope-vs-langgraph-vs-autogen.md  # 历史概念归档，保存 2026、04、22、isotope、vs、langgraph、vs、autogen 的早期设计或外部对比。
│   │   │   ├── 2026-04-22-study-companion-to-isotope-kernel-requirements.md  # 历史概念归档，保存 2026、04、22、study、companion、to、isotope、kernel（核心运行层）、requirements 的早期设计或外部对比。
│   │   │   ├── 2026-04-23-isotope-study-companion-kernel-tension-notes.md  # 历史概念归档，保存 2026、04、23、isotope、study、companion、kernel（核心运行层）、tension、notes 的早期设计或外部对比。
│   │   │   ├── 2026-04-24-isotope-vs-genericagent.md  # 历史概念归档，保存 2026、04、24、isotope、vs、genericagent 的早期设计或外部对比。
│   │   │   ├── 2026-04-24-isotope-vs-petgpt.md  # 历史概念归档，保存 2026、04、24、isotope、vs、petgpt 的早期设计或外部对比。
│   │   │   ├── 2026-05-11-isotope-chatgpt-share-feedback-notes.md  # 历史概念归档，保存 2026、05、11、isotope、chatgpt、share、feedback、notes 的早期设计或外部对比。
│   │   │   ├── README.md  # 归档概念目录说明，解释旧概念材料如何阅读。
│   │   │   └── isotope-vs-hermes-agent.md  # 历史概念归档，保存 isotope、vs、hermes、Agent（智能体） 的早期设计或外部对比。
│   │   ├── release/  # 旧发布草稿归档区。
│   │   │   └── release-draft-v0.1-demo.md  # 历史发布归档，保存 release、draft、demo（演示） 的旧发布草稿。
│   │   ├── current/  # 曾在 current 的来源材料归档区，不作为当前执行入口。
│   │   │   ├── chatgpt审查.md  # 外部审查原文归档，结论已吸收到 current 规则。
│   │   │   ├── 目录结构精简说明.md  # 一次性精简目录快照归档。
│   │   │   ├── agent-task-history.md  # 旧任务队列完整历史归档。
│   │   │   ├── status-history.md  # 旧状态页完整历史归档。
│   │   │   └── 重新梳理目录结构逻辑.md  # 目录结构讨论原文归档。
│   │   ├── plans/  # 旧 v0.1 实施计划归档区。
│   │   │   ├── README.md  # 旧计划归档说明，解释归档原因和阅读规则。
│   │   │   ├── coding-plan-v0.1.md  # 历史计划归档，保存早期 v0.1 编码拆解。
│   │   │   └── implementation-plan-v0.1.md  # 历史计划归档，保存早期 v0.1 最小闭环计划。
│   │   ├── docs-inventory-pre-reorg.md  # 迁移记录归档，保存文档重组前清单和历史计数。
│   │   ├── kernel-decision-log.md  # 历史 kernel 参考，保存早期内核决策日志。
│   │   ├── kernel-mainline-maintenance-mode.md  # 已废止规则归档，解释旧保守维护规则为何不再适用。
│   │   └── kernel-one-pager.md  # 历史 kernel 参考，保存早期一页说明和对象模型。
│   ├── current/  # 当前事实入口目录，记录现在应该优先阅读和执行的项目状态。
│   │   ├── README.md  # 当前文档入口说明，解释 docs/current 的阅读顺序。
│   │   ├── agent-task-queue.md  # Agent 任务队列，记录接下来可推进的工作包。
│   │   ├── application-structure-plan.md  # 应用目录结构计划，说明目标结构和迁移方向。
│   │   ├── codex-supervisor-readonly.md  # Codex Supervisor 只读模式说明，记录只读验证路径。
│   │   ├── compat-proxy-audit.md  # 兼容代理审计，记录兼容入口的保留/清理判断。
│   │   ├── docs-map.md  # 当前文档地图，索引当前事实、架构、功能、评审和归档入口。
│   │   ├── import-map.md  # 导入路径迁移表，记录旧路径到新路径的对应关系。
│   │   ├── naming-and-structure-review.md  # 命名与目录结构审计，记录目录命名问题和调整建议。
│   │   ├── status.md  # 当前项目状态说明，记录最近真实进展和待处理事项。
│   │   ├── supervisor-architecture-migration-table.md  # Supervisor 架构迁移表，记录旧模块到新结构的迁移关系。
│   │   ├── supervisor-capability-details.md  # Supervisor 能力详情，保留完整能力登记。
│   │   ├── supervisor-capability-map.md  # Supervisor 能力地图，梳理 Supervisor 已有能力和入口。
│   │   ├── supervisor-command-reference.md  # Supervisor 命令参考，保留完整命令和长跑流程。
│   │   ├── terminology.md  # 术语索引，用中文解释关键英文技术词和所在层级。
│   │   └── 目录结构最新说明.md  # 当前文件，本仓库 tracked 目录和文件用途说明。
│   ├── features/  # 功能切片文档目录，记录功能设计、验收、摩擦评审和闭环材料。
│   │   ├── demo/  # demo 相关功能材料目录，记录 v0.1/v0.2 演示路径和验收标准。
│   │   │   ├── demo-architecture-v0.1.md  # demo 功能文档，记录 demo（演示）、architecture 的演示路径、验收或准备状态。
│   │   │   ├── demo-entrypoint-v0.1.md  # demo 功能文档，记录 demo（演示）、entrypoint 的演示路径、验收或准备状态。
│   │   │   ├── demo-walkthrough-v0.1.md  # demo 功能文档，记录 demo（演示）、walkthrough 的演示路径、验收或准备状态。
│   │   │   ├── v0.1-demo-acceptance.md  # demo 功能文档，记录 v0.1、demo（演示）、acceptance 的演示路径、验收或准备状态。
│   │   │   ├── v0.2-demo-acceptance.md  # demo 功能文档，记录 v0.2、demo（演示）、acceptance 的演示路径、验收或准备状态。
│   │   │   ├── v0.2-demo-readiness.md  # demo 功能文档，记录 v0.2、demo（演示）、readiness 的演示路径、验收或准备状态。
│   │   │   └── v0.2-demo-scenario.md  # demo 功能文档，记录 v0.2、demo（演示）、scenario 的演示路径、验收或准备状态。
│   │   ├── agent-loop-branch-closure-review.md  # 功能闭环评审，记录 Agent（智能体）、循环执行、branch、closure、评审 的完成情况和剩余风险。
│   │   ├── agent-loop-branch-handoff-checkpoint.md  # 功能文档，记录 Agent（智能体）、循环执行、branch、handoff（移交）、checkpoint（检查点） 的设计、验收或交付背景。
│   │   ├── agent-loop-friction-review.md  # 功能摩擦评审，记录 Agent（智能体）、循环执行、friction、评审 使用中的阻力和改进点。
│   │   ├── agent-loop-planner-adapter-friction-review.md  # 功能摩擦评审，记录 Agent（智能体）、循环执行、planner（规划器）、adapter、friction、评审 使用中的阻力和改进点。
│   │   ├── agent-loop-planner-matrix-friction-review.md  # 功能摩擦评审，记录 Agent（智能体）、循环执行、planner（规划器）、matrix、friction、评审 使用中的阻力和改进点。
│   │   ├── approval-tool-runner-friction-review.md  # 功能摩擦评审，记录 审批、tool（工具）、运行入口、friction、评审 使用中的阻力和改进点。
│   │   ├── artifact-review-flow-closure-review.md  # 功能闭环评审，记录 artifact（产物）、评审、业务流程、closure、评审 的完成情况和剩余风险。
│   │   ├── artifact-review-flow-friction-review.md  # 功能摩擦评审，记录 artifact（产物）、评审、业务流程、friction、评审 使用中的阻力和改进点。
│   │   ├── capability-hub-core-merge-readiness-review.md  # 功能闭环评审，记录 capability（能力）、hub、core、合并流程、readiness、评审 的完成情况和剩余风险。
│   │   ├── capability-runner-thin-shell-closure-review.md  # 功能闭环评审，记录 capability（能力）、运行入口、thin、shell、closure、评审 的完成情况和剩余风险。
│   │   ├── external-snapshot-review-closure-review.md  # 功能闭环评审，记录 外部输入、快照、评审、closure、评审 的完成情况和剩余风险。
│   │   ├── planner-io-validator-spike-review.md  # 功能闭环评审，记录 planner（规划器）、io、校验器、spike、评审 的完成情况和剩余风险。
│   │   ├── planner-matrix-fixture-expansion-review.md  # 功能闭环评审，记录 planner（规划器）、matrix、fixture、expansion、评审 的完成情况和剩余风险。
│   │   ├── planner-restart-pause-fixture-review.md  # 功能闭环评审，记录 planner（规划器）、重启恢复、pause、fixture、评审 的完成情况和剩余风险。
│   │   ├── planner-validated-runner-spike-review.md  # 功能闭环评审，记录 planner（规划器）、validated、运行入口、spike、评审 的完成情况和剩余风险。
│   │   ├── source-artifact-helper-closure-review.md  # 功能闭环评审，记录 源文件/来源、artifact（产物）、helper、closure、评审 的完成情况和剩余风险。
│   │   ├── submit-tool-request-friction-review.md  # 功能摩擦评审，记录 提交动作、tool（工具）、request、friction、评审 使用中的阻力和改进点。
│   │   ├── terminal-backend-closure-review.md  # 功能闭环评审，记录 终端执行、backend、closure、评审 的完成情况和剩余风险。
│   │   ├── usability-friction-round-1-review.md  # 功能摩擦评审，记录 可用性、friction、round、1、评审 使用中的阻力和改进点。
│   │   ├── usability-pressure-test-plan-v0.2.md  # 功能文档，记录 可用性、pressure、测试、plan 的设计、验收或交付背景。
│   │   ├── worker-handoff-app-spike-selection.md  # 功能文档，记录 worker（工作进程）、handoff（移交）、app、spike、selection 的设计、验收或交付背景。
│   │   ├── worker-handoff-helper-closure-review.md  # 功能闭环评审，记录 worker（工作进程）、handoff（移交）、helper、closure、评审 的完成情况和剩余风险。
│   │   └── workspace-binding-helper-friction-review.md  # 功能摩擦评审，记录 workspace（工作区）、binding、helper、friction、评审 使用中的阻力和改进点。
│   └── reviews/  # 评审和复盘目录，记录分支审计、差距分析、闭环复查等证据。
│       ├── agent-loop-chain-closure-2026-05-15.md  # 评审记录，保存 Agent（智能体）、循环执行、chain、closure、2026、05、15 的审计、复盘或差距分析结果。
│       ├── aggressive-remaining-code-intake-refresh-v0.md  # 评审记录，保存 aggressive、remaining、code、intake、refresh 的审计、复盘或差距分析结果。
│       ├── aggressive-remaining-code-review-v0.md  # 评审记录，保存 aggressive、remaining、code、评审 的审计、复盘或差距分析结果。
│       ├── app-spike-coverage-review.md  # 评审记录，保存 app、spike、coverage、评审 的审计、复盘或差距分析结果。
│       ├── branch-audit-initial-2026-05-15.md  # 评审记录，保存 branch、audit、initial、2026、05、15 的审计、复盘或差距分析结果。
│       ├── branch-audit-refresh-2026-05-15.md  # 评审记录，保存 branch、audit、refresh、2026、05、15 的审计、复盘或差距分析结果。
│       ├── branch-cleanup-2026-05-15.md  # 评审记录，保存 branch、cleanup、2026、05、15 的审计、复盘或差距分析结果。
│       ├── checkpoint-history-save-integration-v0.1.md  # 评审记录，保存 checkpoint（检查点）、history、save、集成 的审计、复盘或差距分析结果。
│       ├── controlled-terminal-exec-deep-review-2026-05-15.md  # 评审记录，保存 受控执行、终端执行、exec、deep、评审、2026、05、15 的审计、复盘或差距分析结果。
│       ├── deferred-boundary-review-v0.1.md  # 评审记录，保存 deferred、边界契约、评审 的审计、复盘或差距分析结果。
│       ├── docs-migration-phase-1-dry-run.md  # 评审记录，保存 docs、migration、phase、1、dry、run 的审计、复盘或差距分析结果。
│       ├── docs-migration-plan.md  # 评审记录，保存 docs、migration、plan 的审计、复盘或差距分析结果。
│       ├── external-review-package-v0.2.md  # 评审记录，保存 外部输入、评审、package 的审计、复盘或差距分析结果。
│       ├── first-app-spike-readiness.md  # 评审记录，保存 first、app、spike、readiness 的审计、复盘或差距分析结果。
│       ├── kernel-gap-review-refresh-v0.2.md  # 评审记录，保存 kernel（核心运行层）、gap、评审、refresh 的审计、复盘或差距分析结果。
│       ├── kernel-gap-review-v0.2.md  # 评审记录，保存 kernel（核心运行层）、gap、评审 的审计、复盘或差距分析结果。
│       ├── mainline-idle-checkpoint.md  # 评审记录，保存 mainline、idle、checkpoint（检查点） 的审计、复盘或差距分析结果。
│       ├── post-external-review-checkpoint.md  # 评审记录，保存 post、外部输入、评审、checkpoint（检查点） 的审计、复盘或差距分析结果。
│       ├── post-v0.2-tag-delta.md  # 评审记录，保存 post、tag、delta 的审计、复盘或差距分析结果。
│       ├── retry-cancel-supersede-runtime-closure-review.md  # 评审记录，保存 重试、cancel、替换旧动作、runtime、closure、评审 的审计、复盘或差距分析结果。
│       ├── second-app-spike-selection.md  # 评审记录，保存 second、app、spike、selection 的审计、复盘或差距分析结果。
│       ├── v0.2-cycle-closure-review.md  # 评审记录，保存 v0.2、cycle、closure、评审 的审计、复盘或差距分析结果。
│       ├── v0.2-mid-cycle-review.md  # 评审记录，保存 v0.2、mid、cycle、评审 的审计、复盘或差距分析结果。
│       └── v0.2-next-track-selection.md  # 评审记录，保存 v0.2、next、track、selection 的审计、复盘或差距分析结果。
├── src/  # Python 源码根目录，采用 src/ layout 避免本地导入污染。
│   └── isotope/  # Isotope 主 Python package（包），包含产品核心、功能、平台状态和集成。
│       ├── agents/  # Agent 相关运行逻辑目录，放循环执行和调度能力。
│       │   ├── loop/  # Agent loop（智能体循环）目录，负责规划、步骤驱动和运行控制。
│       │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   ├── control.py  # Agent loop 模块，负责 运行控制。
│       │   │   ├── planner_adapter.py  # Agent loop 模块，负责 planner（规划器）、adapter。
│       │   │   ├── planner_contract.py  # Agent loop 模块，负责 planner（规划器）、contract。
│       │   │   └── step.py  # Agent loop 模块，负责 执行步骤。
│       │   ├── scheduler/  # Agent 调度目录，负责依赖图、容量图、fanout 和目标队列。
│       │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   ├── capacity_graph.py  # Agent scheduler 模块，负责 capacity、graph。
│       │   │   ├── current_batch.py  # Agent scheduler 模块，负责 current batch（当前批次）投影。
│       │   │   ├── dependency_batches.py  # Agent scheduler 模块，负责 依赖关系、batches。
│       │   │   ├── dependency_graph.py  # Agent scheduler 模块，负责 依赖关系、graph。
│       │   │   ├── fanout.py  # Agent scheduler 模块，负责 fanout（并发分发）。
│       │   │   ├── goal_events.py  # Agent scheduler 模块，负责 goal event（目标事件）解析。
│       │   │   └── goal_queue.py  # Agent scheduler 模块，负责 目标队列、queue。
│       │   └── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       ├── apps/  # 应用组合层目录，给 API/CLI 提供可复用的应用入口。
│       │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   └── api.py  # Python 源文件，负责 api 相关逻辑。
│       ├── capabilities/  # capability（能力）系统目录，负责能力目录、能力运行器和工具封装。
│       │   ├── tools/  # 工具实现目录，放可被能力系统或 LLM tool 调用的具体工具。
│       │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   └── terminal.py  # capability tool 模块，提供 终端执行 工具。
│       │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   ├── catalog.py  # capability 系统模块，负责 能力目录。
│       │   └── runner.py  # capability 系统模块，负责 运行入口。
│       ├── core/  # 产品核心流程目录，连接 session、conversation、task、dispatch 和 response。
│       │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   ├── conversation.py  # core 产品主流程模块，负责 conversation。
│       │   ├── dispatch.py  # core 产品主流程模块，负责 分发。
│       │   ├── response.py  # core 产品主流程模块，负责 response。
│       │   ├── session.py  # core 产品主流程模块，负责 session（会话）。
│       │   ├── task.py  # core 产品主流程模块，负责 任务。
│       │   └── time.py  # core 产品主流程模块，负责 time。
│       ├── execution/  # 执行层目录，负责 action 执行、终端执行和执行事件归属。
│       │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   ├── executor.py  # execution 执行层模块，负责 执行器。
│       │   └── terminal_runner.py  # execution 执行层模块，负责 终端执行、运行入口。
│       ├── features/  # 产品功能目录，按 ask、files、projects、supervisor 等用户能力拆分。
│       │   ├── ask/  # 问答/ask 功能目录，负责问题输入、模型池和运行流程。
│       │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   ├── flow.py  # ask 功能的业务流程实现。
│       │   │   ├── pool.py  # ask 功能的资源池/模型池管理。
│       │   │   └── runner.py  # ask 功能的运行入口，通常服务 CLI 或应用层。
│       │   ├── chat/  # 对话功能目录，负责产品聊天流程。
│       │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   └── flow.py  # chat 功能的业务流程实现。
│       │   ├── files/  # 文件功能目录，负责文件处理流程和 CLI 运行入口。
│       │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   ├── flow.py  # files 功能的业务流程实现。
│       │   │   └── runner.py  # files 功能的运行入口，通常服务 CLI 或应用层。
│       │   ├── notifications/  # 通知功能目录，负责提示、铃声事件和通知运行入口。
│       │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   ├── flow.py  # 通知 功能的业务流程实现。
│       │   │   └── runner.py  # 通知 功能的运行入口，通常服务 CLI 或应用层。
│       │   ├── projects/  # 项目功能目录，负责项目创建、项目 workspace 组合和 CLI 入口。
│       │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   ├── flow.py  # 项目 功能的业务流程实现。
│       │   │   ├── runner.py  # 项目 功能的运行入口，通常服务 CLI 或应用层。
│       │   │   └── workspace.py  # 项目 功能的 workspace（工作区）组合逻辑。
│       │   ├── search/  # 搜索功能目录，负责检索流程和命令行入口。
│       │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   ├── flow.py  # 搜索 功能的业务流程实现。
│       │   │   └── runner.py  # 搜索 功能的运行入口，通常服务 CLI 或应用层。
│       │   ├── supervisor/  # Supervisor 功能主目录，负责多 worker 管理、队列、合并、看板和自动化。
│       │   │   ├── commands/  # Supervisor CLI 子命令目录，按 capacity、merge、goal 等命令拆分。
│       │   │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   │   ├── capacity.py  # 实现 Supervisor capacity 子命令，查看或管理 worker 容量。
│       │   │   │   ├── cleanup.py  # 实现 Supervisor cleanup 子命令，清理已结束或失效的工作区。
│       │   │   │   ├── dashboard.py  # 实现 Supervisor dashboard 子命令，启动或查看看板。
│       │   │   │   ├── goal.py  # 实现 Supervisor goal 子命令，管理目标队列。
│       │   │   │   ├── main.py  # Supervisor CLI 子命令分发主入口。
│       │   │   │   ├── merge.py  # 实现 Supervisor merge 子命令，处理合并相关流程。
│       │   │   │   ├── parser.py  # 构建 Supervisor CLI 参数 parser（解析器）。
│       │   │   │   └── promotion.py  # 实现 Supervisor promotion 子命令，处理晋级/推广流程。
│       │   │   ├── planner/  # Supervisor planner 辅助目录，负责目标范围等规划输入。
│       │   │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   │   └── goal_scope.py  # Supervisor 模块，负责 目标队列、scope 相关逻辑。
│       │   │   ├── state/  # Supervisor 状态目录，负责 worker 状态、事件通道和时间工具。
│       │   │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   │   ├── memory_view.py  # Supervisor 模块，负责 memory（记忆）、view 相关逻辑。
│       │   │   │   ├── multi_worker.py  # Supervisor 模块，负责 multi、worker（工作进程） 相关逻辑。
│       │   │   │   ├── time_utils.py  # Supervisor 模块，负责 time、utils 相关逻辑。
│       │   │   │   └── worker_event_channel.py  # Supervisor 模块，负责 worker（工作进程）、event（事件）、channel 相关逻辑。
│       │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   ├── bell_events.py  # 定义 Supervisor 铃声/提示事件，用于状态提醒。
│       │   │   ├── context.py  # 构建 Supervisor 运行上下文，集中携带路径、配置和状态。
│       │   │   ├── current_batch.py  # 读取或描述当前 worker 批次。
│       │   │   ├── daemon.py  # Supervisor 后台循环，用于持续扫描和自动推进。
│       │   │   ├── dashboard_html.py  # 生成 Supervisor Web 看板 HTML。
│       │   │   ├── decision_requests.py  # 记录需要人工决策的请求。
│       │   │   ├── failure_ledger.py  # 维护 Supervisor 失败账本，记录 worker 失败原因。
│       │   │   ├── fanout.py  # 负责把目标分发成多个 worker 工作项。
│       │   │   ├── flow.py  # Supervisor 产品流程编排入口。
│       │   │   ├── goal_planner.py  # 把高层目标拆成可执行的 worker 计划。
│       │   │   ├── goal_queue.py  # 管理 Supervisor 目标队列。
│       │   │   ├── integration_review.py  # 生成或辅助集成评审。
│       │   │   ├── lane_state.py  # 维护执行 lane（通道）状态。
│       │   │   ├── llm_summary.py  # 用 LLM 汇总 worker 状态或结果。
│       │   │   ├── merge_dispatch.py  # 分发合并任务和合并检查。
│       │   │   ├── merge_promotion.py  # 处理合并后的晋级/推广判断。
│       │   │   ├── merge_repair.py  # 处理合并失败后的修复流程。
│       │   │   ├── merge_work_order.py  # 构造合并相关 work order（工作单）。
│       │   │   ├── notifications.py  # Supervisor 通知逻辑。
│       │   │   ├── registry.py  # Supervisor 内部能力或命令注册表。
│       │   │   ├── replan.py  # 目标或 worker 失败后的重新规划逻辑。
│       │   │   ├── runner.py  # Supervisor CLI/运行主入口。
│       │   │   ├── supervisor_llm_pool.toml.example  # 示例 TOML 配置文件，用于说明本地配置格式。
│       │   │   ├── tmux_discovery.py  # 发现 tmux 会话或 pane，用于定位 worker 终端。
│       │   │   ├── web.py  # Supervisor Web 服务入口。
│       │   │   ├── work_order_builder.py  # 构造 worker work order（工作单）。
│       │   │   ├── worker_review.py  # 评审 worker 输出并形成合并/修复建议。
│       │   │   └── worker_test_gate.py  # worker 合并前测试门禁逻辑。
│       │   ├── tasks/  # 任务功能目录，负责任务创建、任务流和 CLI 入口。
│       │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   ├── flow.py  # 任务 功能的业务流程实现。
│       │   │   └── runner.py  # 任务 功能的运行入口，通常服务 CLI 或应用层。
│       │   ├── workbench/  # workbench（工作台）功能目录，负责组合式工作台流程和入口。
│       │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   ├── flow.py  # workbench（工作台） 功能的业务流程实现。
│       │   │   └── runner.py  # workbench（工作台） 功能的运行入口，通常服务 CLI 或应用层。
│       │   └── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       ├── integrations/  # 外部系统集成目录，当前主要集成 Codex。
│       │   ├── codex/  # Codex 集成目录，负责 CLI 调用、服务端适配、session 读取和 live smoke。
│       │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   ├── cli.py  # Codex 集成模块，负责 cli。
│       │   │   ├── live_smoke.py  # Codex 集成模块，负责 live smoke（真实配置冒烟）、smoke（冒烟检查）。
│       │   │   ├── server.py  # Codex 集成模块，负责 服务端。
│       │   │   ├── session_reader.py  # Codex 集成模块，负责 session（会话）、reader。
│       │   │   └── task.py  # Codex 集成模块，负责 任务。
│       │   └── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       ├── interfaces/  # 外部接口目录，放 HTTP 等接口层封装。
│       │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   ├── http.py  # 接口层模块，保留库内 HTTP API facade（门面入口）。
│       │   ├── http_artifact_routes.py  # HTTP route 模块，负责 artifact（产物）相关请求。
│       │   ├── http_dispatch.py  # HTTP dispatch（分发）模块，串接各领域 route handler。
│       │   ├── http_llm_routes.py  # HTTP route 模块，负责 LLM（大模型）相关请求。
│       │   ├── http_product_routes.py  # HTTP route 模块，负责 project/task/file/search/workbench 等产品请求。
│       │   ├── http_responses.py  # HTTP response（响应）模块，负责错误和响应包装。
│       │   ├── http_routes.py  # HTTP route inventory（路由清单）模块，负责 route metadata。
│       │   ├── http_run_routes.py  # HTTP route 模块，负责 run、approval 和 runtime 请求。
│       │   ├── http_serialization.py  # HTTP serialization（序列化）模块，负责响应数据转换。
│       │   ├── http_types.py  # HTTP type（类型）模块，负责轻量响应数据结构。
│       │   └── http_validation.py  # HTTP validation（校验）模块，负责请求体校验。
│       ├── llm/  # LLM 层目录，负责 provider、tool bridge、pool 和容量调用。
│       │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   ├── capacity_calling.py  # LLM 层模块，负责 capacity、calling。
│       │   ├── pool.py  # LLM 层模块，负责 资源池。
│       │   ├── provider.py  # LLM 层模块，负责 模型 provider（供应器）。
│       │   └── tool_bridge.py  # LLM 层模块，负责 tool（工具）、bridge。
│       ├── memory/  # memory（记忆）目录，放记忆相关事件通道和后续记忆能力入口。
│       │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   └── worker_event_channel.py  # memory 模块，负责 worker（工作进程）、event（事件）、channel。
│       ├── platform/  # 平台基础层目录，放事件、schema、状态存储、错误和 ID。
│       │   ├── events/  # 事件模型目录，定义事件结构和兼容校验。
│       │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   ├── event_schema.py  # platform event 模块，负责 event（事件）、schema（数据结构约束）。
│       │   │   └── events.py  # platform event 模块，负责 event（事件）。
│       │   ├── registry/  # 注册表目录，当前主要注册 action 类型。
│       │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   └── actions.py  # platform registry 模块，负责 动作。
│       │   ├── schemas/  # schema 目录，定义 action、artifact、memory、snapshot 等共享数据结构。
│       │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   ├── actions.py  # platform schema 模块，定义 动作 数据结构。
│       │   │   ├── artifacts.py  # platform schema 模块，定义 artifact（产物） 数据结构。
│       │   │   ├── memory.py  # platform schema 模块，定义 memory（记忆） 数据结构。
│       │   │   ├── refs.py  # platform schema 模块，定义 refs 数据结构。
│       │   │   ├── snapshots.py  # platform schema 模块，定义 snapshots 数据结构。
│       │   │   └── tool_protocol.py  # platform schema 模块，定义 tool（工具）、protocol 数据结构。
│       │   ├── state/  # 状态存储目录，负责 event store、checkpoint、ledger 和 projector。
│       │   │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   │   ├── checkpoint_store.py  # platform state 模块，负责 checkpoint（检查点）、store 的持久化或投影。
│       │   │   ├── decision_ledger.py  # platform state 模块，负责 决策、ledger 的持久化或投影。
│       │   │   ├── event_store.py  # platform state 模块，负责 event（事件）、store 的持久化或投影。
│       │   │   ├── failure_ledger.py  # platform state 模块，负责 失败记录、ledger 的持久化或投影。
│       │   │   ├── memory_store.py  # platform state 模块，负责 memory（记忆）、store 的持久化或投影。
│       │   │   └── projector.py  # platform state 模块，负责 状态投影器 的持久化或投影。
│       │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   ├── errors.py  # platform 基础模块，负责 errors。
│       │   └── ids.py  # platform 基础模块，负责 ids。
│       ├── policy/  # policy（策略）目录，当前作为策略能力的 package 边界。
│       │   └── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       ├── rag/  # RAG 目录，负责 ingestion（摄取）和 retrieval（检索）。
│       │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   ├── ingestion.py  # RAG 模块，负责 数据摄取。
│       │   └── retrieval.py  # RAG 模块，负责 检索。
│       ├── runtime/  # runtime（运行时）目录，负责把动作编译并在进程内执行。
│       │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   ├── action_compiler.py  # runtime 模块，负责 动作、动作编译。
│       │   └── in_process.py  # runtime 模块，负责 in、process。
│       ├── workspace/  # workspace（工作区）目录，负责 artifact 捕获和工作区绑定相关能力。
│       │   ├── __init__.py  # Python package 初始化文件，让该目录可以被 import（导入）。
│       │   └── artifacts.py  # workspace 模块，负责 artifact（产物）。
│       ├── __init__.py  # Isotope 主包初始化文件，暴露 package 边界。
│       ├── demo.py  # demo 场景入口，组织 v0.1/v0.2 等演示流程。
│       ├── llm_live_smoke.py  # LLM live smoke 主逻辑，用真实配置检查模型能力是否可用。
│       ├── llm_live_smoke_fakes.py  # LLM live smoke 测试假对象，给测试提供可控替身。
│       └── loop_engine.py  # 通用 loop engine（循环引擎），驱动多步执行流程。
├── tests/  # 测试根目录，放自动化测试。
│   └── isotope/  # Isotope 测试目录，按功能、边界和回归场景覆盖 src/isotope。
│       ├── features/  # 按 feature 分层的测试目录。
│       │   └── supervisor/  # Supervisor feature 的专项测试目录。
│       │       └── test_worker_test_gate.py  # Supervisor feature 专项测试，验证 worker（工作进程）、测试、gate。
│       ├── test_action_cancel_boundary.py  # 自动化测试文件，验证 动作、cancel、边界契约 的行为、边界或回归场景。
│       ├── test_action_chain.py  # 自动化测试文件，验证 动作、chain 的行为、边界或回归场景。
│       ├── test_action_compiler_registry_integration.py  # 自动化测试文件，验证 动作、动作编译、registry（注册表）、集成 的行为、边界或回归场景。
│       ├── test_action_compiler_validation.py  # 自动化测试文件，验证 动作、动作编译、校验 的行为、边界或回归场景。
│       ├── test_action_denied.py  # 自动化测试文件，验证 动作、denied 的行为、边界或回归场景。
│       ├── test_action_failure.py  # 自动化测试文件，验证 动作、失败记录 的行为、边界或回归场景。
│       ├── test_action_lifecycle.py  # 自动化测试文件，验证 动作、生命周期 的行为、边界或回归场景。
│       ├── test_action_registry_version_basis.py  # 自动化测试文件，验证 动作、registry（注册表）、version、basis 的行为、边界或回归场景。
│       ├── test_action_retry_boundary.py  # 自动化测试文件，验证 动作、重试、边界契约 的行为、边界或回归场景。
│       ├── test_action_supersede_boundary.py  # 自动化测试文件，验证 动作、替换旧动作、边界契约 的行为、边界或回归场景。
│       ├── test_action_type_registry.py  # 自动化测试文件，验证 动作、type、registry（注册表） 的行为、边界或回归场景。
│       ├── test_agent_loop_friction_spike.py  # 自动化测试文件，验证 Agent（智能体）、循环执行、friction、spike 的行为、边界或回归场景。
│       ├── test_agent_loop_memory_integration.py  # 自动化测试文件，验证 Agent（智能体）、循环执行、memory（记忆）、集成 的行为、边界或回归场景。
│       ├── test_agent_loop_planner_adapter_spike.py  # 自动化测试文件，验证 Agent（智能体）、循环执行、planner（规划器）、adapter、spike 的行为、边界或回归场景。
│       ├── test_agent_loop_planner_io_validator_spike.py  # 自动化测试文件，验证 Agent（智能体）、循环执行、planner（规划器）、io、校验器、spike 的行为、边界或回归场景。
│       ├── test_agent_loop_planner_matrix_spike.py  # 自动化测试文件，验证 Agent（智能体）、循环执行、planner（规划器）、matrix、spike 的行为、边界或回归场景。
│       ├── test_agent_loop_planner_restart_pause_spike.py  # 自动化测试文件，验证 Agent（智能体）、循环执行、planner（规划器）、重启恢复、pause、spike 的行为、边界或回归场景。
│       ├── test_agent_loop_planner_step_adapter.py  # 自动化测试文件，验证 Agent（智能体）、循环执行、planner（规划器）、执行步骤、adapter 的行为、边界或回归场景。
│       ├── test_agent_loop_planner_validated_runner_spike.py  # 自动化测试文件，验证 Agent（智能体）、循环执行、planner（规划器）、validated、运行入口、spike 的行为、边界或回归场景。
│       ├── test_agent_loop_run_control.py  # 自动化测试文件，验证 Agent（智能体）、循环执行、run、运行控制 的行为、边界或回归场景。
│       ├── test_agent_loop_step_driver.py  # 自动化测试文件，验证 Agent（智能体）、循环执行、执行步骤、driver 的行为、边界或回归场景。
│       ├── test_agent_loop_tick_policy.py  # 自动化测试文件，验证 Agent（智能体）、循环执行、tick、policy（策略） 的行为、边界或回归场景。
│       ├── test_agent_scheduler_capacity_graph.py  # 自动化测试文件，验证 Agent（智能体）、调度器、capacity、graph 的行为、边界或回归场景。
│       ├── test_agent_scheduler_dependency_batches.py  # 自动化测试文件，验证 Agent（智能体）、调度器、依赖关系、batches 的行为、边界或回归场景。
│       ├── test_agent_scheduler_dependency_graph.py  # 自动化测试文件，验证 Agent（智能体）、调度器、依赖关系、graph 的行为、边界或回归场景。
│       ├── test_agent_scheduler_fanout.py  # 自动化测试文件，验证 Agent（智能体）、调度器、fanout（并发分发） 的行为、边界或回归场景。
│       ├── test_agent_worker_lifecycle_boundary.py  # 自动化测试文件，验证 Agent（智能体）、worker（工作进程）、生命周期、边界契约 的行为、边界或回归场景。
│       ├── test_approval_boundary.py  # 自动化测试文件，验证 审批、边界契约 的行为、边界或回归场景。
│       ├── test_approval_lookup_helper.py  # 自动化测试文件，验证 审批、lookup、helper 的行为、边界或回归场景。
│       ├── test_approval_resolution_boundary.py  # 自动化测试文件，验证 审批、resolution、边界契约 的行为、边界或回归场景。
│       ├── test_approval_run_state_invariants.py  # 自动化测试文件，验证 审批、run、状态存储、invariants 的行为、边界或回归场景。
│       ├── test_apps_api_boundary.py  # 自动化测试文件，验证 apps、api、边界契约 的行为、边界或回归场景。
│       ├── test_artifact_content_read_policy.py  # 自动化测试文件，验证 artifact（产物）、content、read、policy（策略） 的行为、边界或回归场景。
│       ├── test_artifact_persistence.py  # 自动化测试文件，验证 artifact（产物）、persistence 的行为、边界或回归场景。
│       ├── test_artifact_provenance_helper.py  # 自动化测试文件，验证 artifact（产物）、provenance、helper 的行为、边界或回归场景。
│       ├── test_artifact_ref.py  # 自动化测试文件，验证 artifact（产物）、ref 的行为、边界或回归场景。
│       ├── test_artifact_review_flow_read_model.py  # 自动化测试文件，验证 artifact（产物）、评审、业务流程、read、模型 的行为、边界或回归场景。
│       ├── test_artifact_review_flow_spike.py  # 自动化测试文件，验证 artifact（产物）、评审、业务流程、spike 的行为、边界或回归场景。
│       ├── test_cancel_runtime_integration_boundary.py  # 自动化测试文件，验证 cancel、runtime、集成、边界契约 的行为、边界或回归场景。
│       ├── test_capability_catalog_core.py  # 自动化测试文件，验证 capability（能力）、能力目录、core 的行为、边界或回归场景。
│       ├── test_capability_catalog_shelves.py  # 自动化测试文件，验证 capability（能力）、能力目录、shelves 的行为、边界或回归场景。
│       ├── test_capability_runner_cli.py  # 自动化测试文件，验证 capability（能力）、运行入口、cli 的行为、边界或回归场景。
│       ├── test_capability_runner_thin_shell.py  # 自动化测试文件，验证 capability（能力）、运行入口、thin、shell 的行为、边界或回归场景。
│       ├── test_capability_search_launch_plan.py  # 自动化测试文件，验证 capability（能力）、搜索、launch、plan 的行为、边界或回归场景。
│       ├── test_checkpoint_assisted_rebuild.py  # 自动化测试文件，验证 checkpoint（检查点）、assisted、rebuild 的行为、边界或回归场景。
│       ├── test_checkpoint_history_fallback.py  # 自动化测试文件，验证 checkpoint（检查点）、history、fallback 的行为、边界或回归场景。
│       ├── test_checkpoint_history_save_boundary.py  # 自动化测试文件，验证 checkpoint（检查点）、history、save、边界契约 的行为、边界或回归场景。
│       ├── test_checkpoint_integrity.py  # 自动化测试文件，验证 checkpoint（检查点）、integrity 的行为、边界或回归场景。
│       ├── test_checkpoint_latest_only_boundary.py  # 自动化测试文件，验证 checkpoint（检查点）、latest、only、边界契约 的行为、边界或回归场景。
│       ├── test_checkpoint_prefix_consistency.py  # 自动化测试文件，验证 checkpoint（检查点）、prefix、consistency 的行为、边界或回归场景。
│       ├── test_checkpoint_state_schema.py  # 自动化测试文件，验证 checkpoint（检查点）、状态存储、schema（数据结构约束） 的行为、边界或回归场景。
│       ├── test_checkpoint_store.py  # 自动化测试文件，验证 checkpoint（检查点）、store 的行为、边界或回归场景。
│       ├── test_checkpoint_versioning_boundary.py  # 自动化测试文件，验证 checkpoint（检查点）、versioning、边界契约 的行为、边界或回归场景。
│       ├── test_ci_workflow.py  # 自动化测试文件，验证 ci、workflow 的行为、边界或回归场景。
│       ├── test_codex_cli_backend.py  # 自动化测试文件，验证 Codex 集成、cli、backend 的行为、边界或回归场景。
│       ├── test_codex_cli_server_wiring.py  # 自动化测试文件，验证 Codex 集成、cli、服务端、wiring 的行为、边界或回归场景。
│       ├── test_codex_live_smoke.py  # 自动化测试文件，验证 Codex 集成、live smoke（真实配置冒烟）、smoke（冒烟检查） 的行为、边界或回归场景。
│       ├── test_codex_session_reader.py  # 自动化测试文件，验证 Codex 集成、session（会话）、reader 的行为、边界或回归场景。
│       ├── test_codex_supervisor_readonly.py  # 自动化测试文件，验证 Codex 集成、Supervisor（监督器）、readonly 的行为、边界或回归场景。
│       ├── test_codex_task_adapter_contract.py  # 自动化测试文件，验证 Codex 集成、任务、adapter、contract 的行为、边界或回归场景。
│       ├── test_codex_task_executor_integration.py  # 自动化测试文件，验证 Codex 集成、任务、执行器、集成 的行为、边界或回归场景。
│       ├── test_codex_task_tool_boundary.py  # 自动化测试文件，验证 Codex 集成、任务、tool（工具）、边界契约 的行为、边界或回归场景。
│       ├── test_compat_proxy_imports.py  # 自动化测试文件，验证 兼容层、proxy、imports 的行为、边界或回归场景。
│       ├── test_controlled_terminal_execution.py  # 自动化测试文件，验证 受控执行、终端执行、execution 的行为、边界或回归场景。
│       ├── test_core_product_flow.py  # 自动化测试文件，验证 core、product、业务流程 的行为、边界或回归场景。
│       ├── test_deferred_capabilities.py  # 自动化测试文件，验证 deferred、capability（能力） 的行为、边界或回归场景。
│       ├── test_delegation_decision_read_model.py  # 自动化测试文件，验证 delegation、决策、read、模型 的行为、边界或回归场景。
│       ├── test_delegation_policy_boundary.py  # 自动化测试文件，验证 delegation、policy（策略）、边界契约 的行为、边界或回归场景。
│       ├── test_demo_entrypoint.py  # 自动化测试文件，验证 demo（演示）、entrypoint 的行为、边界或回归场景。
│       ├── test_demo_trace_mode.py  # 自动化测试文件，验证 demo（演示）、执行 trace（轨迹）、mode 的行为、边界或回归场景。
│       ├── test_demo_v0_2_scenario.py  # 自动化测试文件，验证 demo（演示）、scenario 的行为、边界或回归场景。
│       ├── test_event_envelope_version_boundary.py  # 自动化测试文件，验证 event（事件）、envelope、version、边界契约 的行为、边界或回归场景。
│       ├── test_event_prefix_digest.py  # 自动化测试文件，验证 event（事件）、prefix、digest 的行为、边界或回归场景。
│       ├── test_event_schema_registry_boundary.py  # 自动化测试文件，验证 event（事件）、schema（数据结构约束）、registry（注册表）、边界契约 的行为、边界或回归场景。
│       ├── test_event_schema_version_compatibility.py  # 自动化测试文件，验证 event（事件）、schema（数据结构约束）、version、compatibility 的行为、边界或回归场景。
│       ├── test_event_store.py  # 自动化测试文件，验证 event（事件）、store 的行为、边界或回归场景。
│       ├── test_event_store_hardening.py  # 自动化测试文件，验证 event（事件）、store、hardening 的行为、边界或回归场景。
│       ├── test_executor_event_ownership.py  # 自动化测试文件，验证 执行器、event（事件）、ownership 的行为、边界或回归场景。
│       ├── test_executor_failure_ownership.py  # 自动化测试文件，验证 执行器、失败记录、ownership 的行为、边界或回归场景。
│       ├── test_executor_memory_handler_boundary.py  # 自动化测试文件，验证 执行器、memory（记忆）、handler、边界契约 的行为、边界或回归场景。
│       ├── test_executor_registry_integration.py  # 自动化测试文件，验证 执行器、registry（注册表）、集成 的行为、边界或回归场景。
│       ├── test_external_ingestion_boundary.py  # 自动化测试文件，验证 外部输入、数据摄取、边界契约 的行为、边界或回归场景。
│       ├── test_external_observation_conflicts.py  # 自动化测试文件，验证 外部输入、observation、conflicts 的行为、边界或回归场景。
│       ├── test_external_observation_read_model.py  # 自动化测试文件，验证 外部输入、observation、read、模型 的行为、边界或回归场景。
│       ├── test_external_snapshot_import_helper.py  # 自动化测试文件，验证 外部输入、快照、导入路径、helper 的行为、边界或回归场景。
│       ├── test_external_snapshot_review_read_model.py  # 自动化测试文件，验证 外部输入、快照、评审、read、模型 的行为、边界或回归场景。
│       ├── test_external_snapshot_review_spike.py  # 自动化测试文件，验证 外部输入、快照、评审、spike 的行为、边界或回归场景。
│       ├── test_files_feature_cli.py  # 自动化测试文件，验证 files、feature、cli 的行为、边界或回归场景。
│       ├── test_files_feature_flow.py  # 自动化测试文件，验证 files、feature、业务流程 的行为、边界或回归场景。
│       ├── test_http_api_agent_loop_control.py  # 自动化测试文件，验证 HTTP API、api、Agent（智能体）、循环执行、运行控制 的行为、边界或回归场景。
│       ├── test_http_api_agent_loop_planner_step_adapter.py  # 自动化测试文件，验证 HTTP API、api、Agent（智能体）、循环执行、planner（规划器）、执行步骤、adapter 的行为、边界或回归场景。
│       ├── test_http_api_agent_loop_step_driver.py  # 自动化测试文件，验证 HTTP API、api、Agent（智能体）、循环执行、执行步骤、driver 的行为、边界或回归场景。
│       ├── test_http_api_agent_loop_tick_policy.py  # 自动化测试文件，验证 HTTP API、api、Agent（智能体）、循环执行、tick、policy（策略） 的行为、边界或回归场景。
│       ├── test_http_api_approval_boundary.py  # 自动化测试文件，验证 HTTP API、api、审批、边界契约 的行为、边界或回归场景。
│       ├── test_http_api_approval_lookup.py  # 自动化测试文件，验证 HTTP API、api、审批、lookup 的行为、边界或回归场景。
│       ├── test_http_api_approval_state_read_model.py  # 自动化测试文件，验证 HTTP API、api、审批、状态存储、read、模型 的行为、边界或回归场景。
│       ├── test_http_api_artifact_content_boundary.py  # 自动化测试文件，验证 HTTP API、api、artifact（产物）、content、边界契约 的行为、边界或回归场景。
│       ├── test_http_api_artifact_content_enablement_guard.py  # 自动化测试文件，验证 HTTP API、api、artifact（产物）、content、enablement、guard 的行为、边界或回归场景。
│       ├── test_http_api_boundary.py  # 自动化测试文件，验证 HTTP API、api、边界契约 的行为、边界或回归场景。
│       ├── test_http_api_codex_task_route.py  # 自动化测试文件，验证 HTTP API、api、Codex 集成、任务、route 的行为、边界或回归场景。
│       ├── test_http_api_deferred_routes.py  # 自动化测试文件，验证 HTTP API、api、deferred、routes 的行为、边界或回归场景。
│       ├── test_http_api_demo_smoke.py  # 自动化测试文件，验证 HTTP API、api、demo（演示）、smoke（冒烟检查） 的行为、边界或回归场景。
│       ├── test_http_api_idempotency_boundary.py  # 自动化测试文件，验证 HTTP API、api、幂等性、边界契约 的行为、边界或回归场景。
│       ├── test_http_api_llm_product_chat_route_boundary.py  # 自动化测试文件，验证 HTTP API、api、LLM（大模型）、product、chat、route、边界契约 的行为、边界或回归场景。
│       ├── test_http_api_llm_product_chat_route_contract.py  # 自动化测试文件，验证 HTTP API、api、LLM（大模型）、product、chat、route、contract 的行为、边界或回归场景。
│       ├── test_http_api_llm_provider_route.py  # 自动化测试文件，验证 HTTP API、api、LLM（大模型）、模型 provider（供应器）、route 的行为、边界或回归场景。
│       ├── test_http_api_request_validation.py  # 自动化测试文件，验证 HTTP API、api、request、校验 的行为、边界或回归场景。
│       ├── test_http_api_response_contract.py  # 自动化测试文件，验证 HTTP API、api、response、contract 的行为、边界或回归场景。
│       ├── test_http_api_route_inventory.py  # 自动化测试文件，验证 HTTP API、api、route、inventory 的行为、边界或回归场景。
│       ├── test_http_api_workbench_ask_route.py  # 自动化测试文件，验证 HTTP API、api、workbench（工作台）、ask、route 的行为、边界或回归场景。
│       ├── test_http_error_mapping_boundary.py  # 自动化测试文件，验证 HTTP API、错误处理、mapping、边界契约 的行为、边界或回归场景。
│       ├── test_imported_snapshot_projection_boundary.py  # 自动化测试文件，验证 imported、快照、projection、边界契约 的行为、边界或回归场景。
│       ├── test_isotope_error_taxonomy_boundary.py  # 自动化测试文件，验证 isotope、错误处理、taxonomy、边界契约 的行为、边界或回归场景。
│       ├── test_llm_capacity_calling.py  # 自动化测试文件，验证 LLM（大模型）、capacity、calling 的行为、边界或回归场景。
│       ├── test_llm_live_smoke.py  # 自动化测试文件，验证 LLM（大模型）、live smoke（真实配置冒烟）、smoke（冒烟检查） 的行为、边界或回归场景。
│       ├── test_llm_product_chat_app_entry.py  # 自动化测试文件，验证 LLM（大模型）、product、chat、app、entry 的行为、边界或回归场景。
│       ├── test_llm_product_chat_app_entry_demo_scenario.py  # 自动化测试文件，验证 LLM（大模型）、product、chat、app、entry、demo（演示）、scenario 的行为、边界或回归场景。
│       ├── test_llm_provider.py  # 自动化测试文件，验证 LLM（大模型）、模型 provider（供应器） 的行为、边界或回归场景。
│       ├── test_llm_provider_route_demo_scenario.py  # 自动化测试文件，验证 LLM（大模型）、模型 provider（供应器）、route、demo（演示）、scenario 的行为、边界或回归场景。
│       ├── test_llm_provider_tool_loop.py  # 自动化测试文件，验证 LLM（大模型）、模型 provider（供应器）、tool（工具）、循环执行 的行为、边界或回归场景。
│       ├── test_llm_terminal_tool_loop_demo_scenario.py  # 自动化测试文件，验证 LLM（大模型）、终端执行、tool（工具）、循环执行、demo（演示）、scenario 的行为、边界或回归场景。
│       ├── test_llm_tool_result_loop_demo_scenario.py  # 自动化测试文件，验证 LLM（大模型）、tool（工具）、result、循环执行、demo（演示）、scenario 的行为、边界或回归场景。
│       ├── test_loop_engine.py  # 自动化测试文件，验证 循环执行、engine 的行为、边界或回归场景。
│       ├── test_memory_action_chain_boundary.py  # 自动化测试文件，验证 memory（记忆）、动作、chain、边界契约 的行为、边界或回归场景。
│       ├── test_memory_boundary.py  # 自动化测试文件，验证 memory（记忆）、边界契约 的行为、边界或回归场景。
│       ├── test_memory_query_authorization_boundary.py  # 自动化测试文件，验证 memory（记忆）、query、authorization、边界契约 的行为、边界或回归场景。
│       ├── test_memory_record_checkpoint_boundary.py  # 自动化测试文件，验证 memory（记忆）、record、checkpoint（检查点）、边界契约 的行为、边界或回归场景。
│       ├── test_memory_record_event_boundary.py  # 自动化测试文件，验证 memory（记忆）、record、event（事件）、边界契约 的行为、边界或回归场景。
│       ├── test_memory_record_persistence_boundary.py  # 自动化测试文件，验证 memory（记忆）、record、persistence、边界契约 的行为、边界或回归场景。
│       ├── test_memory_record_shape.py  # 自动化测试文件，验证 memory（记忆）、record、shape 的行为、边界或回归场景。
│       ├── test_memory_record_supersession_boundary.py  # 自动化测试文件，验证 memory（记忆）、record、supersession、边界契约 的行为、边界或回归场景。
│       ├── test_memory_store.py  # 自动化测试文件，验证 memory（记忆）、store 的行为、边界或回归场景。
│       ├── test_model_tool_bridge.py  # 自动化测试文件，验证 模型、tool（工具）、bridge 的行为、边界或回归场景。
│       ├── test_model_tool_bridge_demo_scenario.py  # 自动化测试文件，验证 模型、tool（工具）、bridge、demo（演示）、scenario 的行为、边界或回归场景。
│       ├── test_model_tool_catalog.py  # 自动化测试文件，验证 模型、tool（工具）、能力目录 的行为、边界或回归场景。
│       ├── test_notifications_feature_cli.py  # 自动化测试文件，验证 通知、feature、cli 的行为、边界或回归场景。
│       ├── test_notifications_feature_flow.py  # 自动化测试文件，验证 通知、feature、业务流程 的行为、边界或回归场景。
│       ├── test_packaging_smoke.py  # 自动化测试文件，验证 packaging、smoke（冒烟检查） 的行为、边界或回归场景。
│       ├── test_platform_state_ledgers.py  # 自动化测试文件，验证 platform、状态存储、ledgers 的行为、边界或回归场景。
│       ├── test_policy_decision_validation.py  # 自动化测试文件，验证 policy（策略）、决策、校验 的行为、边界或回归场景。
│       ├── test_policy_grants.py  # 自动化测试文件，验证 policy（策略）、grants 的行为、边界或回归场景。
│       ├── test_policy_profile_version_basis.py  # 自动化测试文件，验证 policy（策略）、profile、version、basis 的行为、边界或回归场景。
│       ├── test_policy_registry_integration.py  # 自动化测试文件，验证 policy（策略）、registry（注册表）、集成 的行为、边界或回归场景。
│       ├── test_project_workspace_demo_scenario.py  # 自动化测试文件，验证 项目、workspace（工作区）、demo（演示）、scenario 的行为、边界或回归场景。
│       ├── test_project_workspace_flow.py  # 自动化测试文件，验证 项目、workspace（工作区）、业务流程 的行为、边界或回归场景。
│       ├── test_projector_checkpoint_creation.py  # 自动化测试文件，验证 状态投影器、checkpoint（检查点）、creation 的行为、边界或回归场景。
│       ├── test_projector_checkpoint_history_save.py  # 自动化测试文件，验证 状态投影器、checkpoint（检查点）、history、save 的行为、边界或回归场景。
│       ├── test_projector_checkpoint_save.py  # 自动化测试文件，验证 状态投影器、checkpoint（检查点）、save 的行为、边界或回归场景。
│       ├── test_projector_event_payload_validation.py  # 自动化测试文件，验证 状态投影器、event（事件）、payload、校验 的行为、边界或回归场景。
│       ├── test_projector_replay.py  # 自动化测试文件，验证 状态投影器、replay 的行为、边界或回归场景。
│       ├── test_projects_feature_cli.py  # 自动化测试文件，验证 项目、feature、cli 的行为、边界或回归场景。
│       ├── test_projects_feature_flow.py  # 自动化测试文件，验证 项目、feature、业务流程 的行为、边界或回归场景。
│       ├── test_real_planner_adapter_contract.py  # 自动化测试文件，验证 real、planner（规划器）、adapter、contract 的行为、边界或回归场景。
│       ├── test_restart_write_helper_run_context.py  # 自动化测试文件，验证 重启恢复、write、helper、run、context 的行为、边界或回归场景。
│       ├── test_retrieval_authorization.py  # 自动化测试文件，验证 检索、authorization 的行为、边界或回归场景。
│       ├── test_retry_runtime_integration_boundary.py  # 自动化测试文件，验证 重试、runtime、集成、边界契约 的行为、边界或回归场景。
│       ├── test_run_completion_invariants.py  # 自动化测试文件，验证 run、completion、invariants 的行为、边界或回归场景。
│       ├── test_run_lifecycle_boundary.py  # 自动化测试文件，验证 run、生命周期、边界契约 的行为、边界或回归场景。
│       ├── test_schema_hardening.py  # 自动化测试文件，验证 schema（数据结构约束）、hardening 的行为、边界或回归场景。
│       ├── test_search_feature_cli.py  # 自动化测试文件，验证 搜索、feature、cli 的行为、边界或回归场景。
│       ├── test_search_feature_flow.py  # 自动化测试文件，验证 搜索、feature、业务流程 的行为、边界或回归场景。
│       ├── test_server_action_registry_wiring.py  # 自动化测试文件，验证 服务端、动作、registry（注册表）、wiring 的行为、边界或回归场景。
│       ├── test_server_checkpoint_boundary.py  # 自动化测试文件，验证 服务端、checkpoint（检查点）、边界契约 的行为、边界或回归场景。
│       ├── test_server_checkpoint_history_save_trigger.py  # 自动化测试文件，验证 服务端、checkpoint（检查点）、history、save、trigger 的行为、边界或回归场景。
│       ├── test_server_checkpoint_save_trigger.py  # 自动化测试文件，验证 服务端、checkpoint（检查点）、save、trigger 的行为、边界或回归场景。
│       ├── test_server_input_validation.py  # 自动化测试文件，验证 服务端、input、校验 的行为、边界或回归场景。
│       ├── test_server_slice.py  # 自动化测试文件，验证 服务端、slice 的行为、边界或回归场景。
│       ├── test_session_lifecycle_boundary.py  # 自动化测试文件，验证 session（会话）、生命周期、边界契约 的行为、边界或回归场景。
│       ├── test_source_artifact_setup_helper.py  # 自动化测试文件，验证 源文件/来源、artifact（产物）、setup、helper 的行为、边界或回归场景。
│       ├── test_submit_action_helper.py  # 自动化测试文件，验证 提交动作、动作、helper 的行为、边界或回归场景。
│       ├── test_supersede_runtime_integration_boundary.py  # 自动化测试文件，验证 替换旧动作、runtime、集成、边界契约 的行为、边界或回归场景。
│       ├── test_supervisor_auto_cleanup_loop.py  # 自动化测试文件，验证 Supervisor（监督器）、auto、cleanup、循环执行 的行为、边界或回归场景。
│       ├── test_supervisor_auto_loop_e2e.py  # 自动化测试文件，验证 Supervisor（监督器）、auto、循环执行、e2e 的行为、边界或回归场景。
│       ├── test_supervisor_automation_actions.py  # 自动化测试文件，验证 Supervisor（监督器）、automation、动作 的行为、边界或回归场景。
│       ├── test_supervisor_capacity_path.py  # 自动化测试文件，验证 Supervisor（监督器）、capacity、path 的行为、边界或回归场景。
│       ├── test_supervisor_current_batch.py  # 自动化测试文件，验证 Supervisor（监督器）、当前事实、batch 的行为、边界或回归场景。
│       ├── test_supervisor_delete_worktree.py  # 自动化测试文件，验证 Supervisor（监督器）、delete、worktree 的行为、边界或回归场景。
│       ├── test_supervisor_failure_ledger.py  # 自动化测试文件，验证 Supervisor（监督器）、失败记录、ledger 的行为、边界或回归场景。
│       ├── test_supervisor_fanout.py  # 自动化测试文件，验证 Supervisor（监督器）、fanout（并发分发） 的行为、边界或回归场景。
│       ├── test_supervisor_goal_planner.py  # 自动化测试文件，验证 Supervisor（监督器）、目标队列、planner（规划器） 的行为、边界或回归场景。
│       ├── test_supervisor_goal_replenishment.py  # 自动化测试文件，验证 Supervisor（监督器）、目标队列、replenishment 的行为、边界或回归场景。
│       ├── test_supervisor_integration_review.py  # 自动化测试文件，验证 Supervisor（监督器）、集成、评审 的行为、边界或回归场景。
│       ├── test_supervisor_lifecycle_trace.py  # 自动化测试文件，验证 Supervisor（监督器）、生命周期、执行 trace（轨迹） 的行为、边界或回归场景。
│       ├── test_supervisor_memory_command.py  # 自动化测试文件，验证 Supervisor（监督器）、memory（记忆）、command 的行为、边界或回归场景。
│       ├── test_supervisor_merge_dispatch.py  # 自动化测试文件，验证 Supervisor（监督器）、合并流程、分发 的行为、边界或回归场景。
│       ├── test_supervisor_merge_dispatch_loop.py  # 自动化测试文件，验证 Supervisor（监督器）、合并流程、分发、循环执行 的行为、边界或回归场景。
│       ├── test_supervisor_merge_refactor_modules.py  # 自动化测试文件，验证 Supervisor（监督器）、合并流程、refactor、modules 的行为、边界或回归场景。
│       ├── test_supervisor_merge_work_order.py  # 自动化测试文件，验证 Supervisor（监督器）、合并流程、work、order 的行为、边界或回归场景。
│       ├── test_supervisor_multi_worker_manager.py  # 自动化测试文件，验证 Supervisor（监督器）、multi、worker（工作进程）、manager 的行为、边界或回归场景。
│       ├── test_supervisor_notification_event_bridge.py  # 自动化测试文件，验证 Supervisor（监督器）、通知、event（事件）、bridge 的行为、边界或回归场景。
│       ├── test_supervisor_replan.py  # 自动化测试文件，验证 Supervisor（监督器）、重新规划 的行为、边界或回归场景。
│       ├── test_supervisor_runner_modularization.py  # 自动化测试文件，验证 Supervisor（监督器）、运行入口、modularization 的行为、边界或回归场景。
│       ├── test_supervisor_work_order_builder.py  # 自动化测试文件，验证 Supervisor（监督器）、work、order、builder 的行为、边界或回归场景。
│       ├── test_supervisor_worker_event_channel.py  # 自动化测试文件，验证 Supervisor（监督器）、worker（工作进程）、event（事件）、channel 的行为、边界或回归场景。
│       ├── test_supervisor_worker_failure_handling.py  # 自动化测试文件，验证 Supervisor（监督器）、worker（工作进程）、失败记录、handling 的行为、边界或回归场景。
│       ├── test_supervisor_worker_review.py  # 自动化测试文件，验证 Supervisor（监督器）、worker（工作进程）、评审 的行为、边界或回归场景。
│       ├── test_tasks_feature_cli.py  # 自动化测试文件，验证 任务、feature、cli 的行为、边界或回归场景。
│       ├── test_tasks_feature_flow.py  # 自动化测试文件，验证 任务、feature、业务流程 的行为、边界或回归场景。
│       ├── test_terminal_backend_adapter_contract.py  # 自动化测试文件，验证 终端执行、backend、adapter、contract 的行为、边界或回归场景。
│       ├── test_terminal_backend_app.py  # 自动化测试文件，验证 终端执行、backend、app 的行为、边界或回归场景。
│       ├── test_terminal_backend_artifact_policy.py  # 自动化测试文件，验证 终端执行、backend、artifact（产物）、policy（策略） 的行为、边界或回归场景。
│       ├── test_terminal_backend_executor_integration.py  # 自动化测试文件，验证 终端执行、backend、执行器、集成 的行为、边界或回归场景。
│       ├── test_terminal_backend_selector_config.py  # 自动化测试文件，验证 终端执行、backend、selector、config 的行为、边界或回归场景。
│       ├── test_terminal_exec_demo_scenario.py  # 自动化测试文件，验证 终端执行、exec、demo（演示）、scenario 的行为、边界或回归场景。
│       ├── test_terminal_tool.py  # 自动化测试文件，验证 终端执行、tool（工具） 的行为、边界或回归场景。
│       ├── test_tool_invocation_runtime_wiring.py  # 自动化测试文件，验证 tool（工具）、invocation、runtime、wiring 的行为、边界或回归场景。
│       ├── test_tool_protocol_boundary.py  # 自动化测试文件，验证 tool（工具）、protocol、边界契约 的行为、边界或回归场景。
│       ├── test_tool_result_event_boundary.py  # 自动化测试文件，验证 tool（工具）、result、event（事件）、边界契约 的行为、边界或回归场景。
│       ├── test_usability_spike_approval_tool_runner.py  # 自动化测试文件，验证 可用性、spike、审批、tool（工具）、运行入口 的行为、边界或回归场景。
│       ├── test_usability_spike_approval_tool_runner_read_model.py  # 自动化测试文件，验证 可用性、spike、审批、tool（工具）、运行入口、read、模型 的行为、边界或回归场景。
│       ├── test_workbench_ask_cli.py  # 自动化测试文件，验证 workbench（工作台）、ask、cli 的行为、边界或回归场景。
│       ├── test_workbench_ask_demo_scenario.py  # 自动化测试文件，验证 workbench（工作台）、ask、demo（演示）、scenario 的行为、边界或回归场景。
│       ├── test_workbench_ask_flow.py  # 自动化测试文件，验证 workbench（工作台）、ask、业务流程 的行为、边界或回归场景。
│       ├── test_workbench_ask_pool_provider.py  # 自动化测试文件，验证 workbench（工作台）、ask、资源池、模型 provider（供应器） 的行为、边界或回归场景。
│       ├── test_workbench_demo_scenario.py  # 自动化测试文件，验证 workbench（工作台）、demo（演示）、scenario 的行为、边界或回归场景。
│       ├── test_workbench_feature_cli.py  # 自动化测试文件，验证 workbench（工作台）、feature、cli 的行为、边界或回归场景。
│       ├── test_workbench_feature_flow.py  # 自动化测试文件，验证 workbench（工作台）、feature、业务流程 的行为、边界或回归场景。
│       ├── test_worker_handoff_error_taxonomy.py  # 自动化测试文件，验证 worker（工作进程）、handoff（移交）、错误处理、taxonomy 的行为、边界或回归场景。
│       ├── test_worker_handoff_helper.py  # 自动化测试文件，验证 worker（工作进程）、handoff（移交）、helper 的行为、边界或回归场景。
│       ├── test_workspace_artifact_capture_boundary.py  # 自动化测试文件，验证 workspace（工作区）、artifact（产物）、capture、边界契约 的行为、边界或回归场景。
│       ├── test_workspace_binding.py  # 自动化测试文件，验证 workspace（工作区）、binding 的行为、边界或回归场景。
│       ├── test_workspace_binding_helper.py  # 自动化测试文件，验证 workspace（工作区）、binding、helper 的行为、边界或回归场景。
│       ├── test_workspace_binding_read_model.py  # 自动化测试文件，验证 workspace（工作区）、binding、read、模型 的行为、边界或回归场景。
│       ├── test_workspace_lease_lifecycle_boundary.py  # 自动化测试文件，验证 workspace（工作区）、lease、生命周期、边界契约 的行为、边界或回归场景。
│       ├── test_workspace_lifecycle_helper.py  # 自动化测试文件，验证 workspace（工作区）、生命周期、helper 的行为、边界或回归场景。
│       └── test_workspace_policy_boundary.py  # 自动化测试文件，验证 workspace（工作区）、policy（策略）、边界契约 的行为、边界或回归场景。
├── .gitignore  # Git 忽略规则，排除虚拟环境、worktree、缓存和本地私有配置。
├── AGENTS.md  # AI 协作规则和仓库开发约束，告诉 Codex/Agent 如何在本项目工作。
├── README.md  # 项目公开入口说明，介绍 Isotope 目标、安装和常用命令。
└── pyproject.toml  # Python 项目元数据和依赖配置，定义 package、脚本入口和测试依赖。
```
